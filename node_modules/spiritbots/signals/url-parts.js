module.exports = {
	'signal':	function(str){
		var parseurl;
		parseurl	= require('url-parse');
		str	= Robots.answer('normalize-url',str);
		return parseurl(str);
	},
	'test': function(fn, done) {
		done();
	},
	'description': 'parses the url string into an object'
}