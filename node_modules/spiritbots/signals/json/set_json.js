var jsonCache	= {};
module.exports	= function(file,json){
	// look in my/configs
	//if ( jsonCache.hasOwnProperty(file) )
	//	return jsonCache[file];
	try{
		if ( !fs.existsSync(file) )
			fs.closeSync(fs.openSync(file,'w'));
		return fs.writeFileSync(file,JSON.stringify(json,null,2));
	}catch(e){
		console.log(arguments.callee.caller.name.toString());
		console.log(arguments.callee.caller.caller.name.toString());
		console.log(arguments.callee.caller.caller.caller.name.toString());
		console.log(e);
	}
}