try {
    let modules = [];
    if (process.argv.length > 2) {
        modules = process.argv.slice(2);
    } else {
        modules = [];
    }
    require(__dirname + '/index.js').unlinkModules(modules);
} catch(e) {
}
process.exit();