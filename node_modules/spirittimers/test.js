'use strict';

/*
describe('SpiritTimers Class', function() {
    let rimraf, spirittimers, spirittimersObj;
    beforeEach(function() {
        rimraf = require('rimraf');
        spirittimers = require('./spirittimers.class.js');
        spirittimersObj = new spirittimers();
    });
    
    it('Should execute timers on timer.', (done) => {
        let timer;
        let obj = {
            'fn': () => {
            }
        };
        spyOn(obj, 'fn').and.callThrough();
        timer = spirittimersObj.createTimerFile(obj.fn, 1000);
        setTimeout(() => {
            expect(obj.fn).toHaveBeenCalled();
            done();
        }, 1500);
    });

    afterEach(function() {
        // spirittimersObj.deleteAllTimers();
    });
});
*/