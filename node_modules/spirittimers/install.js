/**
 * @file
 * This file should create a "timers" folder in the calling directory.
 */
let fs = require('fs');
let path;
if (fs.existsSync(__dirname + '/../../package.json')) {
    path = __dirname + '/../../';
} else {
    path = __dirname + '/';
}
if (!fs.existsSync(path + 'timers')) {
    fs.mkdirSync(path + 'timers');
}